:root{
  --bg:#242424;              /* << full-page background per your request */
  --panel:#1e1e1e;           /* navbar background */
  --text:#E8EAED;
  --muted:#b8b8b8;
  --stroke: rgba(255,255,255,.10);
  --pad: 10px 14px;

  --grad: linear-gradient(90deg,#7928ca 0%, #ff0080 50%, #ffd54a 100%);
}

*{ box-sizing:border-box }
html,body{ height:100% }
body{
  margin:0;
  background: var(--bg);
  color: var(--text);
  font: 15px/1.6 system-ui, -apple-system, Segoe UI, Inter, Roboto, Helvetica, Arial, sans-serif;
}

/* ---------- NAVBAR ---------- */
.navbar{
  position: sticky;
  top: 0;
  z-index: 50;
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  gap: 22px;
  padding: 14px 28px;
  background: var(--panel);
  border-bottom: 1px solid var(--stroke);
}

/* Logo pill */
.logo-pill{
  display: inline-flex;
  align-items: center;
  gap: 12px;
  width: 56px;                         /* collapsed = just avatar */
  overflow: hidden;
  white-space: nowrap;
  padding: 6px 10px;
  border-radius: 40px;
  border: 1px solid var(--stroke);
  background: #1b1b1b;
  text-decoration: none;
  color: var(--text);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.03),
    0 10px 18px rgba(0,0,0,.35);
  transition: width .38s cubic-bezier(.2,.65,.2,1), transform .15s ease, box-shadow .25s ease;
  transform: translateZ(0);
}
.logo-pill:hover{ width: 320px; }
.logo-pill:active{ transform: translateY(1px); }

/* Perfect circular avatar framing */
.avatar-wrap{
  width: 44px; height: 44px;
  border-radius: 999px;
  background: #121212;
  border: 1px solid var(--stroke);
  box-shadow: 0 8px 14px rgba(0,0,0,.45);
  overflow: hidden;
  display: grid; place-items:center;
}
.avatar{
  width: 100%; height: 100%;
  display: block;
  object-fit: cover;        /* even if the SVG renders weirdly, clip perfectly */
  border-radius: inherit;
  transform: translateZ(0);
  transition: transform .35s ease;
}
.logo-pill:hover .avatar{ transform: scale(1.04) rotate(-2deg); }

.logo-pill .logo-text{
  opacity: 0; transform: translateX(8px);
  transition: opacity .25s .08s ease, transform .35s ease;
  font-weight: 600; letter-spacing: .2px;
}
.logo-pill:hover .logo-text{ opacity: 1; transform: none; }

/* Center links */
.main-nav{
  justify-self: center;
  display: flex; align-items: center; gap: 18px;
}
.divider{ width:1px; height:18px; background:var(--stroke); border-radius:1px; }
.nav-link{
  position: relative;
  color: var(--text);
  text-decoration: none;
  padding: 4px 2px;
  transition: color .2s ease;
}
.nav-link::after{
  content:"";
  position:absolute; inset:-6px -10px;
  border-radius:8px;
  background: transparent;
  border:1px solid transparent;
  transition: background .2s ease, border-color .2s ease;
}
.nav-link:hover{ color:#fff; }
.nav-link:hover::after{
  background: rgba(255,255,255,.06);
  border-color: var(--stroke);
}

/* Right CTAs */
.cta-wrap{ justify-self: end; display:flex; align-items:center; gap:16px; }

.get-in-touch{
  color: var(--text); text-decoration: none;
  padding: var(--pad);
  border-radius: 10px;
  border: 1px solid transparent;
  transition: background .25s ease, border-color .25s ease;
  display: flex; align-items: center; gap: 8px;
}
.fly-icon{ width:18px; height:18px; transform: translateX(0) rotate(0); transition: transform .25s ease; }
.get-in-touch:hover{
  background: rgba(255,255,255,.06);
  border-color: var(--stroke);
}
.get-in-touch:hover .fly-icon{ transform: translateX(6px) rotate(10deg); }

/* Gradient outline → fill on hover */
.resume-btn{
  --r: 9px;
  position:relative;
  display:flex; align-items:center; gap:8px;
  padding: 10px 16px;
  border-radius: var(--r);
  color:#f1f3f4; text-decoration:none; font-weight:800; letter-spacing:.5px;
  background:
    linear-gradient(#1a1f28,#12161d) padding-box,
    var(--grad) border-box;
  border: 1px solid transparent;
  overflow: hidden;
  transition: color .25s ease, transform .12s ease;
}
.resume-btn img{ width:18px; height:18px; filter: drop-shadow(0 1px 0 rgba(0,0,0,.6)); }
.resume-btn::before{
  content:""; position:absolute; inset:0; border-radius:inherit;
  background: var(--grad); opacity:0; transition: opacity .3s ease; z-index:0;
}
.resume-btn > *{ position:relative; z-index:1; }
.resume-btn:hover{ color:#111; }
.resume-btn:hover::before{ opacity:1; }
.resume-btn:active{ transform: translateY(1px); }

/* ---------- HERO STUB ---------- */
.hero-stub{
  max-width: 1100px; margin: 56px auto 80px; padding: 0 28px; text-align:center;
}
.hi{
  margin:0; font-weight:900; font-size: 104px; line-height: .9; letter-spacing:-2px;
  background: var(--grad);
  -webkit-background-clip: text; background-clip: text; color: transparent;
}
.tag{ margin-top:12px; color: var(--muted); font-size:20px; }
.hero-ctas{ margin-top:22px; display:flex; gap:14px; justify-content:center; }
.btn{
  text-decoration:none; border-radius:10px; padding:11px 18px; font-weight:700;
  transition: transform .12s ease, opacity .2s ease;
}
.btn.primary{ color:#111; background: var(--grad); }
.btn.ghost  { color:var(--text); border:1px solid var(--stroke); background: rgba(255,255,255,.05); }
.btn:hover{ transform: translateY(-1px); }

/* ---------- FLOATING FABs ---------- */
.fab-stack{
  position: fixed;
  right: 22px;
  bottom: 24px;
  display: flex; flex-direction: column; gap: 12px;
  z-index: 60;
}
.fab{
  width: 56px; height: 56px;
  border-radius: 999px;
  display: grid; place-items: center;
  border: 1px solid var(--stroke);
  background: #2a2a2a;
  box-shadow: 0 12px 24px rgba(0,0,0,.45);
  transition: transform .12s ease, box-shadow .2s ease, opacity .2s ease;
}
.fab img{ width:22px; height:22px; }
.fab:hover{ transform: translateY(-2px); box-shadow: 0 16px 28px rgba(0,0,0,.5); }
.fab:active{ transform: translateY(0); }

.fab-ghost{ background: #2f2f2f; }
.fab-grad{
  background: var(--grad);
  color: #111;
  border-color: transparent;
}

/* Hide FABs on small screens if you want */
@media (max-width: 820px){
  .fab-stack{ display:none; }
}
